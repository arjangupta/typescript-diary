// These declarations need to be used in our ts files
// that have the different methods of calling the setTimeouts
export type StringCallbackFunction = (s: string) => void;
export const MESSAGE_DELAY: number = 1000;